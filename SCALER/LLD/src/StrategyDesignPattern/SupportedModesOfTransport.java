package StrategyDesignPattern;

public enum SupportedModesOfTransport {
    /* This enum eliminates the possible typos that can result to null in functionality.
    * */
    WALK,
    CAR,
    BIKE,
    TRAIN
}
